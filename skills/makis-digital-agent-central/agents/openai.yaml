interface:
  display_name: "Makis Digital Agent Central"
  short_description: "Multi-agent orchestrator — decomposes tasks, dispatches specialized subagents, synthesizes results"
  default_prompt: "Use $makis-digital-agent-central to analyze this task, decompose it into sub-tasks, dispatch specialized subagents via the task tool, then synthesize and verify the result."
