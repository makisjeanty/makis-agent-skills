# Projects CRUD Pattern

- The `projects` module is the main current example for CRUD regressions and safe refactors.
- Tests around this flow should protect routes, admin mutations, and public listing behavior.
