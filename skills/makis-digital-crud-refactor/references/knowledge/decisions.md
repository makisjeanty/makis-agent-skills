# Decisions

- Prefer refactoring inside `src/` instead of migrating to `app/`.
- Keep route contracts stable while refactoring internals.
- Move toward service and repository separation incrementally.
