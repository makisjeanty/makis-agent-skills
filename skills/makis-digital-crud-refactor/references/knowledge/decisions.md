# Decisions

- Keep active development in `src/` because autoload and runtime already point there.
- Keep skills both local and global so the project stays versioned while patterns stay reusable.
- Use one umbrella skill plus specialized skills for clearer activation.
- Prefer validator-compatible frontmatter over spec fields that the local validator rejects.
